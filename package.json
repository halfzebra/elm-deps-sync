{
  "name": "elm-deps-sync",
  "version": "0.0.0-development",
  "description": "Sync elm-package.json between a parent and a sub",
  "main": "index.js",
  "scripts": {
    "test": "mocha tests/functionality.spec.js --reporter spec --timeout 3000",
    "semantic-release": "semantic-release pre && npm publish && semantic-release post"
  },
  "keywords": [
    "elm",
    "cli"
  ],
  "author": {
    "name": "Eduard Kyvenko",
    "email": "eduard.kyvenko@gmail.com"
  },
  "bin": {
    "elm-deps-sync": "./bin/elm-deps-sync-cli.js"
  },
  "files": [
    "bin",
    "index.js"
  ],
  "license": "MIT",
  "dependencies": {
    "chalk": "^1.1.3",
    "commander": "^2.9.0",
    "extend": "^3.0.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "cz-conventional-changelog": "^1.2.0",
    "eslint": "^3.10.0",
    "mocha": "^3.1.2",
    "rimraf": "^2.5.4",
    "semantic-release": "^6.3.2"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/halfzebra/elm-deps-sync.git"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
